{
    "version": "0.2.0",
    "configurations": [
        {
            // used to debug a running process
            "name": ".NET Core Attach",
            "type": "coreclr",
            "request": "attach"
        }
    ]
}

